import 'package:flutter/material.dart';

class KiDayScholar extends StatelessWidget {
  const KiDayScholar({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("DayScholar"),
      ),
      body: Container(
        color: Colors.blue,
      ),
    );
  }
}